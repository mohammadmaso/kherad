PORT=4000
HOST=0.0.0.0
DATABASE_URL=postgres://postgres:postgres@localhost:5432/kherad
GIT_REPO_PATH=./data/git-repo
JWT_SECRET=dev-secret-change-me
GIT_REMOTE_SECRET_KEY=dev-remote-secret-change-me

# GIT_REMOTE_SECRET_KEY also encrypts AI / OCR API keys stored via admin settings.
# Python Docling conversion service (apps/ingest).
INGEST_SERVICE_URL=http://localhost:4100

# Public base URL of this API (no trailing slash). Used as the OAuth redirect_uri
# base for MCP server authorization-code flows.
API_PUBLIC_URL=http://localhost:4000
WEB_ORIGIN=http://localhost:3000
